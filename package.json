{
  "name": "Portfolio",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  }
}
